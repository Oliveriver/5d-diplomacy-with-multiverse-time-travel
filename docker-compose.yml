services:
  frontend:
    build: client/
    ports:
      - 5173:8080
    restart: always
  backend:
    build: server/
    environment:
      - "ConnectionStrings__Database=Data Source=5dDiplomacy.db"
    restart: always
